using UnityEngine;

/// <summary>
/// Composant à attacher à la porte pour gérer son état partagé
/// </summary>
public class DoorState : MonoBehaviour
{
    public bool isOpen = false; // État initial de la porte (ouverte par défaut)
}
